(function(){
"use strict";
const F=window.EVFirebase||{configured:false,currentUser:()=>null,onAuthStateChanged:fn=>fn(null)};
const {CLASSES,SUBJECTS,SECTIONS,DEMO_CATALOG,normalizeCatalog,flattenCatalog}=window.EVCatalog;
const $=id=>document.getElementById(id);
const ui={login:$('loginView'),app:$('appView'),form:$('loginForm'),email:$('email'),pass:$('password'),login:$('loginBtn'),google:$('googleBtn'),forgot:$('forgotBtn'),hint:$('authHint'),content:$('content'),title:$('pageTitle'),date:$('pageDate'),profile:$('profileBtn'),toast:$('toast')};
const state={route:'home',classId:null,subjectId:null,sectionId:null,itemId:null,query:'',catalog:normalizeCatalog(DEMO_CATALOG),profile:{},recent:[],pdf:null};
const RECENT='ezee_student_recent_v10';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cls=c=>`Class ${c}`;const sub=id=>SUBJECTS.find(x=>x.id===id);const sec=id=>SECTIONS.find(x=>x.id===id);
function toast(m){ui.toast.textContent=m;ui.toast.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>ui.toast.classList.remove('show'),2500)}
function dateText(){return new Intl.DateTimeFormat('en-IN',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}
function uid(){return F.currentUser?.()?.uid||'local'}
function loadRecent(){try{state.recent=JSON.parse(localStorage.getItem(RECENT+'_'+uid())||'[]')}catch{state.recent=[]}}
function saveRecent(){localStorage.setItem(RECENT+'_'+uid(),JSON.stringify(state.recent.slice(0,30)))}
function addRecent(it){const k=`${state.classId}|${state.subjectId}|${state.sectionId}|${it.id}`;state.recent=[{classId:state.classId,subjectId:state.subjectId,sectionId:state.sectionId,itemId:it.id},...state.recent.filter(x=>`${x.classId}|${x.subjectId}|${x.sectionId}|${x.itemId}`!==k)].slice(0,30);saveRecent()}
function nav(route,p={}){Object.assign(state,p,{route});render()}
function message(e){return({'auth/invalid-credential':'Email or password is incorrect.','auth/invalid-email':'Enter a valid email.','auth/user-not-found':'Account not found.','auth/wrong-password':'Password is incorrect.','auth/too-many-requests':'Too many attempts. Try again later.','auth/popup-closed-by-user':'Google sign-in cancelled.','auth/unauthorized-domain':'This website domain is not authorized in Firebase.'}[e?.code]||e?.message||'Something went wrong.')}
function busy(b,on,label){b.disabled=on;b.textContent=on?'PLEASE WAIT…':label}
function cardsClasses(){return CLASSES.map(c=>`<button class="class-card" data-class="${c}" type="button"><span class="class-badge">${c}</span><strong>${cls(c)}</strong><small>4 Subjects • 3 Sections each</small></button>`).join('')}
function recentMarkup(n=5){const rows=state.recent.slice(0,n).map(r=>{const it=state.catalog?.[r.classId]?.[r.subjectId]?.[r.sectionId]?.find(x=>x.id===r.itemId);if(!it)return'';return`<button class="recent-card" data-recent="${esc(it.id)}" data-class="${r.classId}" data-subject="${r.subjectId}" data-section="${r.sectionId}" type="button"><span>${sec(r.sectionId)?.icon||'📖'}</span><span><strong>${esc(it.title)}</strong><small>${cls(r.classId)} • ${esc(sub(r.subjectId)?.name||'')}</small></span><b>›</b></button>`}).join('');return rows||`<div class="empty-card"><div>📖</div><strong>No recent material</strong><p>Open a note or worksheet to build your history.</p></div>`}
function viewHome(){const all=flattenCatalog(state.catalog),protectedCount=all.filter(x=>x.sectionId!=='worksheet').length,works=all.length-protectedCount;return`<section class="hero-card"><div><span class="hero-kicker">WELCOME BACK</span><h3>${esc(state.profile.name||F.currentUser?.()?.displayName||'Student')} 👋</h3><p>Classes 6–10 • 4 Subjects • Protected learning content</p></div><button class="hero-link" data-action="classes" type="button">Browse Classes →</button></section><div class="home-grid"><div class="stat-card"><span>🎓</span><small>Classes</small><strong>5</strong></div><div class="stat-card"><span>📚</span><small>Subjects</small><strong>4</strong></div><div class="stat-card"><span>🔒</span><small>Protected Notes</small><strong>${protectedCount}</strong></div><div class="stat-card"><span>📄</span><small>Worksheets</small><strong>${works}</strong></div></div><div class="section-heading"><h3>Choose your class</h3><button class="text-btn" data-action="classes" type="button">View all</button></div><div class="class-grid">${cardsClasses()}</div><div class="section-heading"><h3>Recently opened</h3><button class="text-btn" data-action="recent" type="button">View recent</button></div>${recentMarkup(5)}`}
function viewClasses(){return`<div class="page-intro"><h3>Select a class</h3><p>Only Classes 6–10 are available.</p></div><div class="class-grid large">${cardsClasses()}</div>`}
function viewSubjects(){return`<button class="back-btn" data-action="classes" type="button">← Back to Classes</button><div class="title-block"><p>${cls(state.classId)}</p><h3>Choose a subject</h3></div><div class="subject-grid">${SUBJECTS.map(s=>`<button class="subject-card" data-subject="${s.id}" type="button"><span class="subject-icon">${s.icon}</span><strong>${s.name}</strong><small>Detailed • Short • Worksheet</small></button>`).join('')}</div>`}
function viewSections(){return`<button class="back-btn" data-action="subjects" type="button">← Back to Subjects</button><div class="title-block"><p>${cls(state.classId)} • ${esc(sub(state.subjectId).name)}</p><h3>Choose material section</h3></div><div class="section-grid">${SECTIONS.map(s=>`<button class="section-card" data-section="${s.id}" type="button"><span class="section-icon">${s.icon}</span><strong>${s.name}</strong><small>${s.protected?'🔒 Read only inside app':'📥 Download permitted'}</small></button>`).join('')}</div>`}
function viewMaterials(){const s=sub(state.subjectId),x=sec(state.sectionId),rows=state.catalog?.[state.classId]?.[state.subjectId]?.[state.sectionId]||[];return`<button class="back-btn" data-action="sections" type="button">← Back to Sections</button><div class="title-block"><p>${cls(state.classId)} • ${esc(s.name)}</p><h3>${x.name}</h3><span class="section-note">${x.protected?'🔒 Download and Print controls are intentionally unavailable.':'📥 Worksheet download is enabled.'}</span></div><div class="materials-list">${rows.map(it=>`<button class="material-card" data-item="${esc(it.id)}" type="button"><span class="material-icon">${x.icon}</span><span class="material-body"><strong>${esc(it.title)}</strong><small>${x.protected?'Open in protected reader':'Preview / download worksheet'}</small></span><span class="material-arrow">›</span></button>`).join('')||`<div class="empty-card"><div>${x.icon}</div><strong>No content published</strong><p>Add content to the Firebase catalog to show it here.</p></div>`}</div>`}
function viewSearch(){const q=state.query.trim().toLowerCase();const rows=q?flattenCatalog(state.catalog).filter(x=>`${x.title} ${x.classId} ${sub(x.subjectId).name} ${sec(x.sectionId).name}`.toLowerCase().includes(q)).slice(0,60):[];return`<div class="page-intro"><h3>Search material</h3><p>Search across all five classes.</p></div><input id="searchBox" class="search-input" value="${esc(state.query)}" placeholder="🔎 Search chapters, class, subject..."><div class="search-results">${q?(rows.length?rows.map(x=>`<button class="result-card" data-result="${esc(x.id)}" data-class="${x.classId}" data-subject="${x.subjectId}" data-section="${x.sectionId}" type="button"><span>${sec(x.sectionId).icon}</span><span><strong>${esc(x.title)}</strong><small>${cls(x.classId)} • ${esc(sub(x.subjectId).name)} • ${esc(sec(x.sectionId).name)}</small></span>›</button>`).join(''):`<div class="empty-card"><strong>No results</strong><p>Try another keyword.</p></div>`):`<div class="empty-card"><div>🔎</div><strong>Search your study material</strong><p>Try “Light”, “Algebra” or “Class 10”.</p></div>`}</div>`}
function viewRecent(){return`<div class="page-intro"><h3>Recently opened</h3><p>Your history is saved per account on this device.</p></div>${recentMarkup(30)}`}
function viewProfile(){const u=F.currentUser?.();return`<div class="panel profile-card"><div class="profile-avatar">${esc((state.profile.name||u?.displayName||'S')[0]).toUpperCase()}</div><h3>${esc(state.profile.name||u?.displayName||'Student')}</h3><p>${esc(state.profile.email||u?.email||'')}</p><label for="profileName">Display name</label><input id="profileName" class="search-input" value="${esc(state.profile.name||u?.displayName||'')}" placeholder="Your name"><button id="saveProfile" class="primary-btn" type="button">SAVE PROFILE</button><button id="logoutBtn" class="danger-btn" type="button">LOG OUT</button></div>`}
function findCurrent(){return(state.catalog?.[state.classId]?.[state.subjectId]?.[state.sectionId]||[]).find(x=>x.id===state.itemId)}
function viewReader(){const it=findCurrent(),x=sec(state.sectionId);if(!it)return`<div class="empty-card"><strong>Material not found</strong><button class="primary-btn" data-action="materials" type="button">Go back</button></div>`;return`<button class="back-btn" data-action="materials" type="button">← Back to ${x.name}</button><section class="reader-card"><div class="reader-head"><div><p>${cls(state.classId)} • ${esc(sub(state.subjectId).name)}</p><h3>${esc(it.title)}</h3></div><span class="access-pill ${x.protected?'protected':'download'}">${x.protected?'PROTECTED':'WORKSHEET'}</span></div>${x.protected?`<div id="readerShell" class="reader-shell"><div id="readerStatus" class="reader-status"><div class="spinner"></div><strong>Loading protected note…</strong><p>The PDF is rendered inside this app. Normal Download and Print controls are removed.</p></div><canvas id="pdfCanvas" class="pdf-canvas"></canvas><div id="pdfToolbar" class="pdf-toolbar hidden"><button id="prev" class="mini-btn" type="button">‹</button><span id="pageInfo">1 / 1</span><button id="next" class="mini-btn" type="button">›</button><button id="minus" class="mini-btn" type="button">−</button><button id="plus" class="mini-btn" type="button">+</button><button id="fullscreen" class="mini-btn" type="button">⛶</button></div></div>`:`<div class="worksheet-box"><div class="big-icon">📄</div><h3>Worksheet</h3><p>Preview in a new tab or download the PDF.</p><div class="reader-actions"><button id="previewWorksheet" class="secondary-btn" type="button">PREVIEW</button><button id="downloadWorksheet" class="primary-btn" type="button">DOWNLOAD</button></div></div>`}</section>`}
function render(){ui.title.textContent={home:'Home',classes:'Classes',subjects:'Subjects',sections:'Sections',materials:'Materials',reader:'Reader',search:'Search',recent:'Recent',profile:'Profile'}[state.route]||'Home';ui.date.textContent=dateText();document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.route===state.route));const v={home:viewHome,classes:viewClasses,subjects:viewSubjects,sections:viewSections,materials:viewMaterials,reader:viewReader,search:viewSearch,recent:viewRecent,profile:viewProfile}[state.route]||viewHome;ui.content.innerHTML=v();bind();}
function bind(){
  ui.content.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>{const a=b.dataset.action;if(a==='classes')nav('classes');if(a==='subjects')nav('subjects');if(a==='sections')nav('sections');if(a==='materials')nav('materials');if(a==='recent')nav('recent')});
  ui.content.querySelectorAll('[data-class]').forEach(b=>b.onclick=()=>nav('subjects',{classId:b.dataset.class}));
  ui.content.querySelectorAll('[data-subject]').forEach(b=>b.onclick=()=>nav('sections',{subjectId:b.dataset.subject}));
  ui.content.querySelectorAll('[data-section]').forEach(b=>b.onclick=()=>nav('materials',{sectionId:b.dataset.section}));
  ui.content.querySelectorAll('[data-item]').forEach(b=>b.onclick=()=>nav('reader',{itemId:b.dataset.item}));
  ui.content.querySelectorAll('[data-recent]').forEach(b=>b.onclick=()=>nav('reader',{classId:b.dataset.class,subjectId:b.dataset.subject,sectionId:b.dataset.section,itemId:b.dataset.recent}));
  ui.content.querySelectorAll('[data-result]').forEach(b=>b.onclick=()=>nav('reader',{classId:b.dataset.class,subjectId:b.dataset.subject,sectionId:b.dataset.section,itemId:b.dataset.result}));
  const sb=$('searchBox'); if(sb) sb.oninput=e=>{state.query=e.target.value;render();$('searchBox')?.focus();$('searchBox')?.setSelectionRange(state.query.length,state.query.length)};
  $('saveProfile')?.addEventListener('click',async()=>{const name=$('profileName').value.trim();if(!name)return toast('Enter a display name.');try{state.profile={...state.profile,name,email:F.currentUser?.()?.email||''};if(F.currentUser?.()?.uid)await F.saveProfile(uid(),state.profile);await F.updateProfile(F.currentUser?.(),{displayName:name});toast('Profile saved');render()}catch(e){toast(e?.message||'Profile save failed')}});
  $('logoutBtn')?.addEventListener('click',()=>F.logout());
  $('prev')?.addEventListener('click',()=>{if(state.pdf?.page>1){state.pdf.page--;paint()}});$('next')?.addEventListener('click',()=>{if(state.pdf?.page<state.pdf?.doc?.numPages){state.pdf.page++;paint()}});$('minus')?.addEventListener('click',()=>{state.pdf.scale=Math.max(.7,state.pdf.scale-.1);paint()});$('plus')?.addEventListener('click',()=>{state.pdf.scale=Math.min(2,state.pdf.scale+.1);paint()});$('fullscreen')?.addEventListener('click',()=>$('readerShell')?.requestFullscreen?.());
  $('downloadWorksheet')?.addEventListener('click',downloadWorksheet);$('previewWorksheet')?.addEventListener('click',previewWorksheet);
  if(state.route==='reader'){const it=findCurrent();if(it){addRecent(it);if(sec(state.sectionId)?.protected)loadPdf(it)}}
}
async function loadPdf(it){const status=$('readerStatus');try{const blob=await F.readPdf(it.filePath);window.pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.js';const doc=await window.pdfjsLib.getDocument({data:await blob.arrayBuffer()}).promise;state.pdf={doc,page:1,scale:1};status.classList.add('hidden');$('pdfToolbar').classList.remove('hidden');await paint()}catch(e){status.innerHTML=`<div class="empty-card"><div>⚠️</div><strong>Could not open this note</strong><p>${esc(e?.message||'The document is unavailable.')}</p></div>`}}
async function paint(){const r=state.pdf,canvas=$('pdfCanvas');if(!r||!canvas)return;const page=await r.doc.getPage(r.page),vp=page.getViewport({scale:r.scale});canvas.width=vp.width;canvas.height=vp.height;await page.render({canvasContext:canvas.getContext('2d'),viewport:vp}).promise;$('pageInfo').textContent=`${r.page} / ${r.doc.numPages}`}
async function currentBlob(){const it=findCurrent();if(!it?.filePath)throw Error('Worksheet is not published yet.');return F.readPdf(it.filePath)}
async function downloadWorksheet(){try{const blob=await currentBlob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=(findCurrent().title||'worksheet')+'.pdf';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500);toast('Worksheet downloaded')}catch(e){toast(e?.message||'Download failed')}}
async function previewWorksheet(){try{const blob=await currentBlob(),url=URL.createObjectURL(blob),w=window.open('','_blank');if(!w)throw Error('Allow pop-ups to preview the worksheet.');w.document.write(`<iframe src="${url}" title="Worksheet" style="border:0;width:100%;height:100vh"></iframe>`);w.document.close();setTimeout(()=>URL.revokeObjectURL(url),120000)}catch(e){toast(e?.message||'Preview failed')}}
ui.form.addEventListener('submit',async e=>{e.preventDefault();const email=ui.email.value.trim(),pass=ui.pass.value;if(!email||!pass)return toast('Enter email and password.');busy(ui.login,true,'LOGIN');try{await F.signIn(email,pass)}catch(err){busy(ui.login,false,'LOGIN');toast(message(err))}});
ui.google.addEventListener('click',async()=>{busy(ui.google,true,'Continue with Google');try{await F.googleSignIn()}catch(e){busy(ui.google,false,'Continue with Google');toast(message(e))}});
ui.forgot.addEventListener('click',async()=>{const email=ui.email.value.trim();if(!email)return toast('Enter your email first.');try{await F.resetPassword(email);toast('Password reset email sent.')}catch(e){toast(message(e))}});
ui.profile.addEventListener('click',()=>nav('profile'));document.querySelectorAll('.nav-item').forEach(b=>b.addEventListener('click',()=>nav(b.dataset.route)));
document.addEventListener('contextmenu',e=>{if(e.target.closest('#readerShell'))e.preventDefault()});document.addEventListener('keydown',e=>{if(document.querySelector('#readerShell')&&(e.ctrlKey||e.metaKey)&&['p','s','u'].includes(e.key.toLowerCase())){e.preventDefault();toast('This action is disabled in the protected reader.')}});
F.onAuthStateChanged(async user=>{if(!user){ui.app.classList.add('hidden');ui.login.classList.remove('hidden');busy(ui.login,false,'LOGIN');busy(ui.google,false,'Continue with Google');return}ui.login.classList.add('hidden');ui.app.classList.remove('hidden');try{state.profile=await F.loadProfile(user.uid)||{name:user.displayName||'Student',email:user.email||''}}catch{state.profile={name:user.displayName||'Student',email:user.email||''}}try{state.catalog=normalizeCatalog(await F.loadCatalog())}catch{state.catalog=normalizeCatalog(DEMO_CATALOG)}loadRecent();render()});
})();
