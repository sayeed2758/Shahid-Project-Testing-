/*
 * ADMIN ACCESS CONFIGURATION
 *
 * Change this email to the Firebase account that should manage PDFs.
 * Keep the same email in database.rules.json and firebase/storage.rules.
 * Security Rules are the real protection; this list only controls the UI gate.
 */
window.EV_ADMIN_CONFIG={
  emails:["creativesayeedd@gmail.com"]
};
