{
  "name": "ezee-vision-champua-learning-portal",
  "private": true,
  "version": "0.3.0-drive-gateway",
  "description": "EZEE VISION CHAMPUA student learning portal with Google Drive content gateway",
  "scripts": {
    "serve": "python -m http.server 8080",
    "firebase:deploy": "firebase deploy --only database:rules"
  },
  "dependencies": {
    "firebase": "12.18.0"
  }
}
